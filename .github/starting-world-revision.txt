Map1 qualification 11: group numeric meshes and ANI assets without suspending material or prefab imports.
Includes local original return portal, safe combat cleanup, and qualified single-executable delivery.
Three import persistence/recovery tests join original corpus and local portal tests.
Do not cancel the already running immutable Map1 build; this revision is queued after it.
Only a real Player that passes NPC/mission/five-enemy/reward verification may be delivered.
No synthetic world fallback. Main and original DATA remain unchanged.
